see [here](<README.md#todo-features>) for feature todos.<br>
this is strictly for dev todos.

- consider replacing explicit panics with Results or ?T
- replace special function cases e.g. func_pow2 with func(Pow2Int)
- lily.mem functions take arbitrary data rather than only []u8